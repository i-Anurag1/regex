import { Boxes, CheckCircle2, Database, GitBranch, Server, ShieldCheck, Workflow } from 'lucide-react';
const layers=[['01','Input layer','React + TypeScript','Regex editor, examples, navigation, history and export controls.',Boxes],['02','Presentation layer','React Flow + Tailwind','Automata graphs, AST tree, tables, simulation trace and responsive workspace.',Workflow],['03','API layer','FastAPI + Pydantic','Typed endpoints for compilation, simulation, batch testing and assistant requests.',Server],['04','Compiler core','Python modules','Lexer → parser → AST → Thompson NFA → DFA → minimization → simulator.',GitBranch],['05','Verification','Pytest + CI','Unit and API coverage plus compile checks and frontend production builds.',ShieldCheck]] as const;
export default function Architecture(){return <main className="info-page"><div className="info-hero"><div className="eyebrow"><Boxes size={14}/> SYSTEM ARCHITECTURE</div><h1>One project.<br/><span>Every compiler stage.</span></h1><p>A clean separation between the visual interface, HTTP API and compiler algorithms keeps the system easy to inspect, test and extend.</p></div><section className="architecture-flow">{layers.map(([num,title,tech,desc,Icon],i)=><div className="layer-row" key={title}><span className="layer-num">{num}</span><div className="layer-icon"><Icon size={20}/></div><div className="layer-copy"><div><h2>{title}</h2><span>{tech}</span></div><p>{desc}</p></div>{i<layers.length-1&&<div className="layer-line"/>}</div>)}</section><section className="quality-grid"><div className="quality-card"><CheckCircle2/><div><h3>Production-oriented delivery</h3><p>Docker Compose runs the frontend and backend together. Nginx serves the built React application and proxies API requests.</p></div></div><div className="quality-card"><Database/><div><h3>Inspectable data</h3><p>The compilation response carries tokens, AST, NFA, DFA, minimized DFA, transition data and algorithm steps to the UI.</p></div></div></section><section className="api-card"><div className="card-kicker">API SURFACE</div><div className="endpoint"><span className="method post">POST</span><code>/api/compile</code><span>Compile regex into every intermediate representation</span></div><div className="endpoint"><span className="method post">POST</span><code>/api/simulate</code><span>Trace one input through the minimized DFA</span></div><div className="endpoint"><span className="method post">POST</span><code>/api/batch-simulate</code><span>Evaluate multiple input strings</span></div><div className="endpoint"><span className="method post">POST</span><code>/api/assistant</code><span>Optional natural-language regex helper</span></div></section></main>}
