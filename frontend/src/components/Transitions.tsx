import { ArrowRight, Table2 } from 'lucide-react';
import type { Graph } from '../types';
export default function Transitions({ graph }: { graph: Graph }) { return <section className="data-card"><div className="data-card-head compact"><div><div className="card-kicker"><Table2 size={13}/> TRANSITION FUNCTION</div><h3>Machine transitions</h3></div><span className="count-pill">{graph.transitions.length} edges</span></div><div className="table-wrap"><table><thead><tr><th>From</th><th>Input symbol</th><th>To</th></tr></thead><tbody>{graph.transitions.map((e,i)=><tr key={i}><td><span className="state-pill">q{e.from}</span></td><td><code>{e.symbol === 'epsilon' ? 'ε' : e.symbol}</code></td><td><span className="state-pill">q{e.to}</span> <ArrowRight size={13} className="muted" /></td></tr>)}</tbody></table></div></section>; }
