import { Braces } from 'lucide-react';
import type { Token } from '../types';
export default function TokenTable({ tokens }: { tokens: Token[] }) { return <section className="data-card"><div className="data-card-head"><div><div className="card-kicker"><Braces size={13}/> LEXICAL ANALYSIS</div><h3>Token stream</h3><p>Each token records its type, source value and exact character span.</p></div><span className="count-pill">{tokens.length} tokens</span></div><div className="table-wrap"><table><thead><tr><th>#</th><th>Kind</th><th>Value</th><th>Source span</th></tr></thead><tbody>{tokens.map((t,i)=><tr key={i}><td className="muted">{String(i+1).padStart(2,'0')}</td><td><span className="token-kind">{t.kind}</span></td><td><code>{JSON.stringify(t.value)}</code></td><td><span className="span-pill">{t.start} → {t.end}</span></td></tr>)}</tbody></table></div></section>; }
